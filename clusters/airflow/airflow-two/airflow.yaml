---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: airflow-two
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infrastructure/airflow/resourceset
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  postBuild:
    substitute:
      namespace: airflow-two
      workers_replicas: "2"
      dags_git_repo: "https://github.com/ShubhamTatvamasi/fluxcd"
      dags_git_branch: "main"
      dags_git_subpath: "dags"

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openebs
  namespace: openebs
spec:
  interval: 10m
  chartRef:
    kind: HelmChart
    name: openebs
    namespace: openebs
  values:
    localpv-provisioner:
      hostpathClass:
        isDefaultClass: true
    openebs-crds:
      csi:
        volumeSnapshots:
          enabled: false
    alloy:
      enabled: false
    loki:
      enabled: false
    engines:
      local:
        lvm:
          enabled: false
        zfs:
          enabled: false
      replicated:
        mayastor:
          enabled: false

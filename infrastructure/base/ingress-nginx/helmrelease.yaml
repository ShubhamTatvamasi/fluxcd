---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ingress-nginx
  namespace: ingress-nginx
spec:
  interval: 10m
  chartRef:
    kind: HelmChart
    name: ingress-nginx
    namespace: ingress-nginx
  values:
    controller:
      ingressClassResource:
        default: true

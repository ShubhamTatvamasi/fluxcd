---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: traefik
    namespace: traefik
  values:
    providers:
      kubernetesCRD:
        allowExternalNameServices: true
    ports:
      web:
        http:
          redirections:
            entryPoint:
              to: websecure
              scheme: https
              permanent: true
    ingressRoute:
      dashboard:
        enabled: true
        matchRule: Host(`traefik.k8s.shubhamtatvamasi.com`)
        entryPoints:
          - websecure
        middlewares:
          - name: oauth2-proxy-auth
            namespace: traefik
          - name: oauth2-proxy-errors
            namespace: traefik
        tls:
          secretName: traefik-tls

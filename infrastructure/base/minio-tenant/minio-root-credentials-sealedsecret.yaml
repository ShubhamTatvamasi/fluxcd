---
# PLACEHOLDER — not yet sealed against this cluster's sealed-secrets controller.
# Generate the real encryptedData with:
#   kubectl create secret generic minio-root-credentials -n minio-tenant \
#     --from-literal=config.env='export MINIO_ROOT_USER=minio
#   export MINIO_ROOT_PASSWORD=<strong-password>' \
#     --dry-run=client -o yaml | kubeseal --format yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/cluster-wide: "true"
  name: minio-root-credentials
  namespace: minio-tenant
spec:
  encryptedData:
    config.env: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      annotations:
        sealedsecrets.bitnami.com/cluster-wide: "true"
      name: minio-root-credentials
    type: Opaque

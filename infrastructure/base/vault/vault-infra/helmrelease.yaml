---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault-secrets-webhook
  namespace: vault-infra
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: vault-secrets-webhook
    namespace: vault-infra

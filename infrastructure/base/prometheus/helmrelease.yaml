---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prometheus
  namespace: prometheus
spec:
  interval: 1m
  chartRef:
    kind: HelmChart
    name: prometheus
    namespace: prometheus
  values:
    server:
      image:
        tag: "v3.11.0"

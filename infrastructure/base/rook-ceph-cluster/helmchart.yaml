---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: rook-ceph-cluster
  namespace: rook-ceph
spec:
  interval: 10m
  chart: rook-ceph-cluster
  version: "v1.20.6"
  sourceRef:
    name: rook-ceph
    kind: HelmRepository

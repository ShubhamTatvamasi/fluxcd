---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rook-ceph
  namespace: rook-ceph
spec:
  interval: 10m
  chartRef:
    kind: HelmChart
    name: rook-ceph
    namespace: rook-ceph
  values:
    resources: null
    monitoring:
      enabled: true
    csi:
      cephcsi:
        tag: v3.17.1

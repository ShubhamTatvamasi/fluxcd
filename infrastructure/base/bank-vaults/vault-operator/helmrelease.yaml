---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault-operator
  namespace: vault-operator
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: vault-operator
    namespace: vault-operator
  values:
    crds:
      install: true

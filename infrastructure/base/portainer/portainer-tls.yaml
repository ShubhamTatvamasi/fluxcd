---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: portainer-tls
  namespace: portainer
spec:
  force: false
  interval: 10m0s
  path: ./secrets/base/shubhamtatvamasi-tls
  postBuild:
    substitute:
      tls_secret_name: portainer-tls
      namespace: portainer
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  timeout: 10m
  wait: true

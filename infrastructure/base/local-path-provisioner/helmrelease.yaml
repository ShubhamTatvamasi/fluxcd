---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: local-path-provisioner
  namespace: local-path-storage
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: local-path-provisioner
    namespace: local-path-storage
  values:
    storageClass:
      defaultClass: true

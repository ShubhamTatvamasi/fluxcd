---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: postgres
  namespace: ${namespace}
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: postgres
    namespace: ${namespace}
  values:
    auth:
      password: "postgres"

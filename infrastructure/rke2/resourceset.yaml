---
apiVersion: fluxcd.controlplane.io/v1
kind: ResourceSet
metadata:
  name: shubhamtatvamasi-tls
  namespace: flux-system
spec:
  inputs:
    - namespace: "airflow"
    - namespace: "dex"
    - namespace: "flux-system"
    - namespace: "gitea"
    - namespace: "harbor"
    - namespace: "monitoring"
    - namespace: "portainer"
  resourcesTemplate: |
    ---
    apiVersion: v1
    kind: Namespace
    metadata:
      name: << inputs.namespace >>
    ---
    apiVersion: kustomize.toolkit.fluxcd.io/v1
    kind: Kustomization
    metadata:
      name: shubhamtatvamasi-tls
      namespace: << inputs.namespace >>
    spec:
      interval: 10m0s
      path: ./secrets/base/shubhamtatvamasi-tls
      prune: true
      wait: true
      timeout: 10m
      targetNamespace: << inputs.namespace >>
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      postBuild:
        substitute:
          namespace: << inputs.namespace >>

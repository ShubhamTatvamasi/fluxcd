---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: postgres
  namespace: airflow
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: postgres
    namespace: airflow
  values:
    auth:
      password: "postgres"

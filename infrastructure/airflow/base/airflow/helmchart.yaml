---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: airflow
  namespace: airflow-base
spec:
  interval: 10m
  chart: airflow
  version: "1.20.0"
  sourceRef:
    name: airflow
    kind: HelmRepository
    namespace: airflow-base
